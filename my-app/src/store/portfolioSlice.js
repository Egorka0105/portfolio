import {createSlice} from "@reduxjs/toolkit";


const myPortfolio = createSlice({
    name:"myPortfolio",
    initialState: [],
    reducers: {

    }
})

export const {} = myPortfolio.actions;
export default myPortfolio.reducer;
